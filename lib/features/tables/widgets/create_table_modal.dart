import 'package:flutter/material.dart';

class CreateTableModal extends StatelessWidget {
  const CreateTableModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
